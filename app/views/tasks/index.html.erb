<h1>Index</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><label><input type="checkbox"><span><%= link_to task.title, task_path(task) %></span></label></li>


<%= link_to "Delete",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure to delete #{task.title}?"}
 %>
   <% end %>
</ul>
<%# # il faut interpoler dans la fenetre Are you sure ?
# {@task(param[:id])} %>
